{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Docker: Node.js",
      "type": "node",
      "request": "attach",
      "port": 9229,
      "restart": true,
      "address": "localhost",
      "localRoot": "${workspaceFolder}/student_service",
      "remoteRoot": "/app",
      "preLaunchTask": "Run Docker Container (Debug Mode)"
    }
  ]
}
